apiVersion: v1
kind: Service
metadata:
  labels:
    name: ebs-iops-reporter
  name: ebs-iops-reporter
  namespace: openshift-monitoring
spec:
  ports:
  - name: http-main
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    name: ebs-iops-reporter
  sessionAffinity: None
  type: ClusterIP
